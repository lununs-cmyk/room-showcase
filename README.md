# Room Showcase v4

세 가지 방 형식과 최대 5장 이미지 업로드를 지원합니다.

- 제한 채팅형: 이미지 최대 5장 + 제한형 1:1 채팅
- 카메라 셔터형: 이미지 최대 5장 + 셔터/플래시/전환/원본 저장
- SNS 피드형: 이미지 최대 5장 + 캐러셀/좋아요/본문
- 이미지 한 장 최대 25MB
- 원본은 R2, 메타데이터는 D1에 저장

## 기존 프로젝트 업그레이드

1. 이 ZIP의 내용물을 GitHub 저장소 루트에 덮어쓰고 커밋합니다.
2. Cloudflare D1 Console에서 `migrations/0005_room_types_and_multi_images.sql`의 SQL을 위에서부터 **한 문장씩** 실행합니다.
3. 기존 바인딩을 확인합니다.
   - D1: `DB`
   - R2: `IMAGES`
   - Secret: `ADMIN_PASSWORD`
4. Pages 프로젝트를 재배포합니다.
5. `/admin/`에서 형식을 선택해 방을 만듭니다.

## 새로 설치

D1에 `migrations/0001_initial.sql` 전체를 실행하고 위 바인딩 3개를 설정합니다.

## 주의

- `ALTER TABLE ... already exists` 오류는 해당 열이 이미 있다는 뜻이므로 다음 문장으로 넘어가면 됩니다.
- 기존 v3 단일 이미지는 마이그레이션 마지막 INSERT가 `room_images`로 복사합니다.
- R2 버킷은 Public으로 열 필요가 없습니다.

## v4.1 관리자 화면 개선
- 방 형식별 실시간 미리보기
- 선택한 형식에 필요한 입력 메뉴만 표시
- SNS 좋아요 수를 방 생성 시 자동 랜덤 저장
- 입력, 라벨, 버튼 정렬 규칙 통일
- 기존 방 목록에서 iframe 코드와 실제 미리보기 다시 확인

이 업데이트는 추가 D1 마이그레이션이 필요하지 않습니다. 기존 파일을 덮어쓴 뒤 Cloudflare Pages가 새 커밋을 배포하면 적용됩니다.


## v4.2 개선사항
- 채팅형 이미지는 상단 갤러리가 아니라 이미지 게시자의 메시지에 순서대로 1장씩 첨부됩니다.
- 채팅형 메시지 제한은 첨부 이미지 수보다 작게 설정할 수 없습니다.
- 카메라형에 초기 블러, 초점 그리드, 흔들림, 플래시, 자동 노출, 샤프닝, 비네팅/그레인, 스티커, EXIF 표시, 재촬영, 원본 저장, 촬영 후 사진 전환 효과를 복구했습니다.
- SNS형에 게시물 등장/스캔 효과, NEW POST 배지, 좋아요/더블클릭 하트, 원본 저장, 이미지 드래그 전환, 자동 댓글과 좋아요 증가 효과를 복구했습니다.


## v4.4 추가 변경
- 채팅 메시지에 전송 시각 표시
- 채팅 입력자 선택 영역을 하단에 고정
- 카메라형과 SNS형 높이 축소로 iframe 세로 스크롤 제거
- SNS 공유 버튼 제거
- SNS 저장 버튼으로 현재 이미지 원본 다운로드
- SNS 이미지를 좌우 드래그하여 전환


## v4.5 변경사항

- 공개 방 페이지의 바깥 배경을 투명으로 변경
- iframe 삽입 시 게시판의 흰색 또는 사용자 지정 배경이 그대로 보이도록 조정
- 채팅 카드, 카메라 화면, SNS 게시물 자체 디자인은 유지

## v4.7 변경 사항
- iframe 권장 높이를 모든 방 유형에서 800px로 통일
- 채팅형 너비를 기존 720px로 복구
- 카메라 셔터형 하단 잘림 및 외부 세로 스크롤 문제 수정
- 관리자 카메라 미리보기를 실제 생성 화면의 구조와 비율에 맞게 재작성
- SNS 댓글 표현을 이모티콘과 특수문자 중심으로 다양화
- 댓글 영역 상단에 자연스럽게 사라지는 투명 그라데이션 추가

## v4.9 변경 사항
- 채팅형과 SNS형 관리자 미리보기를 실제 생성 화면 구조와 동일하게 조정
- 채팅형 메시지 스크롤바를 얇은 그라데이션 스타일로 변경
- 카메라 셔터음을 지정된 Daum CDN 음원으로 고정
- 방 형식 변경 시 방 코드 자동 재생성
- 카메라 프레임을 450×780, 사진 영역을 450×450으로 고정해 하단 찌그러짐 방지
- SNS 댓글 영역과 피드 정보 영역 순서 교환 및 전체 하단 그라데이션 적용


## v4.9 변경사항
- 채팅형 및 SNS형 관리자 미리보기 스타일 복구 및 실제 화면과 정렬
- 촬영 후 저장/다시 촬영 버튼이 셔터 위치에서 교체되도록 수정
- SNS 하단 시간 텍스트 제거, 피드 높이 축소, 댓글 투명도 강화


## v5.0 변경사항
- 카메라 셔터 효과음을 외부 URL 대신 프로젝트 내부 `assets/shutter.mp3` 파일로 포함했습니다.
- 촬영 버튼 클릭 시 `/assets/shutter.mp3`를 직접 재생하므로 외부 서버의 확장자·CORS·핫링크 제한 영향을 받지 않습니다.


## v5.1
- SNS 피드형 관리자 미리보기의 아바타가 가로로 늘어나는 문제 수정
- 실제 생성 화면에도 1:1 원형 비율 보호 규칙 추가


## v5.2 changes
- SNS title follows room title.
- Optional SNS avatar upload, falling back to the first post image.
- Camera image selection uses Photo text buttons only.
- Chat file box with gallery and bulk download.


## v5.3 채팅 역할별 링크
채팅방은 같은 대화 데이터를 공유하는 게시자용 링크와 게시판 주인용 비밀 링크를 각각 발급합니다. 게시판 주인용 링크의 key 값은 외부에 공개하지 마세요. 파일박스는 두 화면 모두에서 사용할 수 있습니다.


## v5.5 변경 사항
- 채팅 메시지 영역의 좌우 여백과 긴 문자열 줄바꿈을 보강했습니다.
- 카메라/SNS 방은 생성 완료 시 단일 주소와 단일 iframe만 표시합니다.
- 게시자/게시판 주인 전용 링크 분리는 채팅형에만 적용됩니다.


## v5.6 수정
- 관리자 iframe 확인 모달의 닫기 버튼과 배경 클릭 닫기 기능을 복구했습니다.
- 주인 iframe 복사 버튼 ID 불일치로 후속 이벤트 등록이 중단되던 문제를 수정했습니다.
- ESC 키 닫기 기능을 유지합니다.

## v5.7 변경사항
- 방 생성 완료 영역에서 역할별 주소와 iframe은 채팅형에만 표시됩니다.
- 카메라 셔터형과 SNS 피드형은 단일 방 주소와 단일 iframe만 표시됩니다.
- 공통 버튼의 `display:inline-flex` 규칙이 HTML `hidden` 속성을 덮어쓰던 문제를 수정했습니다.
